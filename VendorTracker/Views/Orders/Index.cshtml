@using VendorTracker.Models;

<h1>Orders</h1>

@if (@Model.Count == 0)
{
    <h3>There are no orders in the list.</h3>
}

@foreach (Order order in Model)
{
    <h3><a href='/orders/@order.Id'>@order.Name</a></h3>
}

<p><a href='/'>Back home.</a></p>
